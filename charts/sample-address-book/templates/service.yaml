apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-address-book-service.fullname" . }}
  labels:
    {{- include "sample-address-book-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-address-book-service.selectorLabels" . | nindent 4 }}